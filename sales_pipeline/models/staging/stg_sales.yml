version: 2

models:
  - name: stg_sales
    description: "Staging table for raw sales data"
    columns:
    - name: order_id
      description: "Unique identifier for each sales record"
      tests:
        - not_null 
  